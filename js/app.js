const headerMain = document.querySelector(".menu");

window.onscroll = function () {
  showProduct();
};

function showProduct() {
  if (document.body.scrollTop > 50 || document.documentElement.scrollTop > 50) {
  }
}
